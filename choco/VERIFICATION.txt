VERIFICATION

This package is built from source and published by the celq maintainers.

Generated by `CertUtil -hashfile celq.exe SHA256`:

CELQ_SHA256_EXE

---

Instructions to verify:

0. If you do not have GnuPG installed:
   choco install gnupg

1. Obtain the public key from GitHub and import it:

   Download the key manually from:
   https://github.com/IvanIsCoding/celq/blob/main/.github/keys/chocolatey-public-key.asc?raw=true
   Then run:
   gpg --import chocolatey-public-key.asc

2. Validate that this VERIFICATION.txt file has not been tampered with:

   gpg --verify VERIFICATION.txt

   You should see: `Good signature from "celq (For Chocolatey releases) <get-celq@proton.me>"`
   Possibly followed by `WARNING: This key is not certified with a trusted signature!`

   Note: if you make any minor edit to VERIFICATION.txt the check will fail.
   - DO NOT change from UTF-8 to UTF-16 
   - DO NOT change newline characters

3. Confirm that the provided SHA256 matches that of celq.exe:

   CertUtil -hashfile celq.exe SHA256

   Compare the output hash with the SHA256 listed in VERIFICATION.txt.
   They must match exactly.