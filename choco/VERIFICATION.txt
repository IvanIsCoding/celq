VERIFICATION

This package is built from source and published by the celq maintainers.

Generated by `CertUtil -hashfile celq.exe SHA256`:

CELQ_SHA256_EXE

---

To verify the binary, confirm that celq.exe's SHA256 matches
the SHA256 reported on the attestation URL:
CELQ_ATTESTATION_URL


Alternatively, this can be done via the command line:

0. If you do not have the GitHub CLI installed:
   choco install gh

1. Authenticate the GitHub CLI, if you haven't done so:
   gh auth login

2. Verify the attestation of celq.exe:
   
   gh attestation verify celq.exe --repo IvanIsCoding/celq

This message has been signed with celq's private key. To verify it has not been tampered,
you can use the following publick key shared in the repository:
https://github.com/IvanIsCoding/celq/blob/main/.github/keys/chocolatey-public-key.asc?raw=true
